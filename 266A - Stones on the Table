#include<iostream>
#include<stdlib.h>
#include<stack>
using namespace std;
 
int main()
{
    int i , n, c= 0, j;
    string s;
    cin>> n;
    cin >> s;
    
    for(i = 0;  i < n ; i++)
{
        if(s[i] == s[i+1])
        {
            c++;
        }
    }
    cout<< c;
}
